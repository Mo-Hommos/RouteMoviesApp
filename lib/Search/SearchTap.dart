import 'package:flutter/material.dart';

class SearchTap extends StatelessWidget {
  static const routename = "Searchap";
  const SearchTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
